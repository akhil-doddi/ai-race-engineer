# AI Race Engineer — Python Dependencies
# Pin versions to ensure reproducible installs across environments.
# Run: pip install -r requirements.txt

# OpenAI API client — GPT-4o-mini for race engineer responses
openai>=1.30.0

# Speech recognition — converts driver voice to text via Google Speech API
SpeechRecognition>=3.10.0

# Text-to-speech — macOS voice output for engineer responses
pyttsx3>=2.90

# Environment variable loading — reads API keys from .env file
python-dotenv>=1.0.0

# FastF1 — historical F1 race data replay (Phase 3 FastF1 replay source)
# Provides lap-by-lap telemetry, tyre data, gaps, and track status for any
# completed race session from 2018 onwards.
# Docs: https://docs.fastf1.dev
fastf1>=3.3.0
